{
  "name": "todo-backend",
  "version": "1.0.0",
  "description": "Simple Todo App backend (Express, no database, in-memory storage)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
